/** Shared layout tokens — deployer runs inside the onboarding overlay (fixed viewport). */
export const deployerStepActionsClass =
    'flex shrink-0 flex-wrap items-center justify-between gap-2 border-t border-border/40 pt-3';

export const deployerStepBodyClass = 'min-h-0 flex-1 overflow-hidden';
