/**
 * FiveM NUI Chromium version — keep in sync with `nui/vite.config.ts` `build.target`
 * and `panel/vite.config.ts` `build.target`.
 */
export const FIVEM_CEF_CHROME_VERSION = 103;

/**
 * Human-readable label for error messages and dev tooling.
 */
export const FIVEM_CEF_LABEL = `FiveM CEF (Chrome ${FIVEM_CEF_CHROME_VERSION})`;
